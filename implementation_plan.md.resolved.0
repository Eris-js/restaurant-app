# Restaurant Website Implementation Plan

## Goal
Build a complete, responsive restaurant website.
**Update**: User provided an existing `index.html` with Bootstrap. We will port this to Next.js, breaking it into components while adhering to the Tailwind CSS requirement where possible (or integrating Bootstrap if strictly necessary to preserve exact looks without full rewrite). *Decision: We will re-implement the UI structure using Tailwind to match the "Technology: Tailwind" requirement, ensuring a modern codebase.*

## Tech Stack
- **Framework**: Next.js 14+ (App Router)
- **Styling**: Tailwind CSS
- **Database**: MongoDB (via Mongoose)
- **Icons**: Lucide React / FontAwesome (if needed for migration)

## Database Connection
- **File**: `lib/db.ts`
- **Env**: `.env.local` (User reported "bad auth", implies existing Mongo requires credentials or connection string fix).

## Components (Ported from `index.html`)

### Layout
- `Navbar.tsx` (Header)
- `Footer.tsx`

### Sections (Home Page)
1.  **HeroSection** (Slider): "TỔ CHỨC TIỆC CƯỚI..."
2.  **AboutSection** (Info): "Giới thiệu"
3.  **GallerySection**: "Thư viện hình ảnh" (Swiper integration)
4.  **MenuSection**: "MENU" (Grid of items)
5.  **SafetySection**: "Vấn Đề Vệ Sinh..."
6.  **ReviewsSection** (Evaluate): "Ý kiến khách hàng"
7.  **MapSection**: Google Maps iframe

## Data Models (Schema)
*(Same as before)*
- Slider, Gallery, Post, Promotion

## API Routes
*(Same as before)*

## Next Steps
1.  Fix MongoDB connection.
2.  Create component structure.
3.  Migrate HTML content to React components.
